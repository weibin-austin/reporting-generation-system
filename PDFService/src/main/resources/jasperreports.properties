# Substitute a default font instead of failing when the template's font is
# not installed on the host (e.g. Times New Roman on Linux runners/containers).
net.sf.jasperreports.awt.ignore.missing.font=true
