# Local development against LocalStack (see LocalStackConfig)
cloud.aws.credentials.accessKey=test
cloud.aws.credentials.secretKey=test
app.aws.endpoint=http://localhost:4566
spring.data.mongodb.uri=mongodb://localhost:27017/pdf_service
# Use the real MongoDB above; the embedded flapdoodle mongod (3.5.5) has no ARM64 build
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.embedded.EmbeddedMongoAutoConfiguration
